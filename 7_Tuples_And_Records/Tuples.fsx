// Tuples provide a convenient way to return more than one value from the function.
let scalarMultiply s x y z = (s * x, s * y, s * z)

// Evaluate the following in emacs as "region" by selecting it and pressing C-c C-r.

scalarMultiply 2 4 6 8
